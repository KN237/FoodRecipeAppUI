import 'package:flutter/material.dart';

SizedBox addVerticalSpace(double number) => SizedBox(
      height: number,
    );

SizedBox addHorizontalSpace(double number) => SizedBox(
      width: number,
    );
